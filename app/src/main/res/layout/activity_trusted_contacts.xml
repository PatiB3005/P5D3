<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E9EEF5">

    <!-- TOOLBAR: TYLKO STRZAŁKA -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarTrusted"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:background="#2B6CB0"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:title=""
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- TYTUŁ POD PASKIEM -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Zaufane kontakty"
        android:textSize="28sp"
        android:textColor="#1A202C"
        android:fontFamily="@font/montserrat_bold"
        android:gravity="center"
        android:layout_marginTop="16dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        app:layout_constraintTop_toBottomOf="@id/toolbarTrusted"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ADD NEW ROW -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/addNewRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingTop="14dp"
        android:paddingBottom="14dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/plusCircle"
            android:layout_width="46dp"
            android:layout_height="46dp"
            app:cardCornerRadius="23dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="#2B6CB0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="+"
                android:textSize="26sp"
                android:textColor="#FFFFFF"
                android:fontFamily="@font/montserrat_bold"
                android:gravity="center" />
        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/tvAddNew"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Dodaj nowy"
            android:textSize="18sp"
            android:textColor="#2B6CB0"
            android:fontFamily="@font/montserrat_bold"
            android:layout_marginStart="14dp"
            app:layout_constraintStart_toEndOf="@id/plusCircle"
            app:layout_constraintTop_toTopOf="@id/plusCircle"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/tvMax"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Maks. 5 kontaktów"
            android:textSize="14sp"
            android:textColor="#4A5568"
            android:fontFamily="@font/montserrat_regular"
            android:layout_marginStart="14dp"
            app:layout_constraintStart_toEndOf="@id/plusCircle"
            app:layout_constraintTop_toBottomOf="@id/tvAddNew"
            app:layout_constraintEnd_toEndOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Brak zaufanych kontaktów.\nKliknij „Dodaj nowy”."
        android:textSize="18sp"
        android:textColor="#4A5568"
        android:fontFamily="@font/montserrat_regular"
        android:gravity="center"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/addNewRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTrusted"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/addNewRow"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
